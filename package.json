{
  "name": "stillpoint",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:budget": "node tools/audit-budget.mjs",
    "build": "node build.mjs",
    "package": "npm run build && node tools/package.mjs",
    "screenshots": "npm run build && playwright test --config=store/playwright.config.ts",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "license": "Apache-2.0"
}
